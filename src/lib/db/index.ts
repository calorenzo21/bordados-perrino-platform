/**
 * Database Repositories
 *
 * This folder contains all database access logic (repositories).
 * Each entity should have its own repository file following the pattern: <entity>.repo.ts
 *
 * Conventions:
 * - All database calls go through repositories
 * - Repositories use the Supabase client from @/lib/supabase/server
 * - Never call Supabase directly from components, pages, or API routes
 * - Each repository exports typed functions for CRUD operations
 *
 * Example structure:
 * - orders.repo.ts
 * - clients.repo.ts
 * - users.repo.ts
 */

export {};

